name: E2E Tests

on:
  push:
    branches:
      - main
      - dev
  pull_request:
    branches:
      - main

jobs:
  e2e:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Set up Node.js
      uses: actions/setup-node@v3
      with:
        node-version: '' # or the version you are using

    - name: Install dependencies
      run: npm install

    - name: Run e2e tests
      run: npm run test:e2e 